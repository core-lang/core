fun main(): Unit {
  let some = Some[String]("some");
  let none = None[String];

  assert(some.accept(|val: String|: Bool { val == "some" }).isSome());
  assert(some.accept(|val: String|: Bool { val == "none" }).isNone());
  assert(none.accept(|val: String|: Bool { val == "some" }).isNone());
}
